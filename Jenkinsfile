pipeline {

    agent any
    stages {
        stage('Build') {
            echo "this is a build stage"
        }
        stages {
        stage('Test') {
            echo "this is a build Test"
        }
        stages {
        stage('Deploy') {
            echo "this is a build Deploy"
        }
    }
}
    }
}
